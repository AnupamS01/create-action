name: MAIN_PULL_REQUEST

on: pull_request

jobs:
  create-artifact:
    runs-on: ubuntu-latest

    steps:
      - name: Create _site
        run: |
          echo "; echo 'attacker controlled code'; #" > _site.zip

      - name: Upload artifact
        uses: actions/upload-artifact@v2
        with:
          name: _site
          path: ./_site.zip
